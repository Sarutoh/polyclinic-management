<div class='container d-flex align-items-center border border-5 border-light rounded-3 mb-2'>
  <%= link_to(appointment, class: 'text-decoration-none text-dark') do %>
    <div>
      Doctor <strong><%= appointment.doctor.full_name %></strong>
    </div>
    <div>
      Appointment date <strong><%= appointment.appointment_date.to_fs(:long_ordinal) %></strong>
    </div>
    <div> Click to see more info </div>
  <% end %>
</div>
